# Copy this file to .env and adjust if your backend runs elsewhere.
# The backend mounts all routes under /api (see backend/src/index.ts),
# and by default listens on port 4000 (see backend/.env -> PORT).
VITE_API_BASE_URL=http://localhost:4000/api
